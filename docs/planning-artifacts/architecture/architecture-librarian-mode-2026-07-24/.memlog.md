---
scope: librarian-mode feature slice (SPEC-librarian-mode)
purpose: build substrate for epics/stories
altitude: feature
updated: 2026-07-24T21:43
---

- (direction) Fast path chosen; spine is the only deliverable; audience = build agents/epics; input = SPEC-librarian-mode (10 CAPs) + its 3 companions; brownfield anchors = docs/planning-artifacts/architecture.md (v0.1 locked) and docs/project-context.md - inherited, not re-decided.
- (decision) Paradigm: hub-and-spoke over file contracts. Global hub (registry + global skills + lumi-hub) with fully self-contained per-wiki spokes; every hub-to-spoke interaction is subprocess CLI + JSON or plain file additions - never imports, never shared in-memory state. Extends the existing single-source projection + thin-CLI-toolbelt architecture without modifying it.
- (decision) AD boundary rule: global layer may touch a wiki only via (a) cd wiki && node _lumina/scripts/*.mjs subprocess, (b) new-file additions under raw/tmp|raw/discovered, (c) reads. Prevents cross-wiki writes and engine-version coupling.
- (decision) Registry I/O owned by one new module on the installer side (src/installer/registry.js [ASSUMPTION: exact filename]); atomicWrite; home resolved via os.homedir() never tilde-string; LUMINA_HOME env override for tests; schema {version:1} with refuse-newer guard mirroring manifest schemaVersion behavior. No other code path reads or writes wikis.json.
- (decision) Resolver determinism: exact matching only, after NFC + casefold + diacritic-strip normalization; priority key > name > aliases; zero or multiple matches = exit 2 with candidates in JSON error. Fuzzy/semantic selection is exclusively the model's job over wikis list output - never in CLI. Prevents non-reproducible routing.
- (decision) Version-correct engine invocation: doctor/verify/any per-wiki op from the global layer executes THAT wiki's own _lumina/scripts (subprocess, cwd=wiki), never the repo or global copy. Prevents schema drift across wikis on different Lumina versions.
- (decision) Canonical-layout single source: required dirs/skeleton as pure data in one installer-side module (src/installer/layout.js [ASSUMPTION]); consumed by install, wikis verify, doctor fix. Prevents two divergent definitions of a conforming wiki.
- (decision) Additive-only repair invariant: fix mode may only create missing entries from layout data (mkdir/seed); an existing path is never opened for write; enforced by exists-then-skip and covered by a byte-identical test on pre-existing files.
- (decision) Skill projection: SKILL.md sources stay single-source; AI-agent installs render through the existing template engine with agent_host flag; the routing preamble is ONE shared partial (src/templates/partials/librarian-preamble.md [ASSUMPTION: path]) injected at render; classic IDE render MUST be byte-identical to source - enforced by a CI check. Prevents preamble drift and classic-install pollution.
- (decision) Ownership-manifest deletion rule (CAP-9): every skills-writing path derives its owned set from a manifest (project: existing skills-manifest.csv; global: per-platform manifest under ~/.lumina [ASSUMPTION: agents/<platform>-manifest.json]); delete only owned-and-deselected canonicalIds; whole-directory wipes of any skills dir are banned; foreign entries untouchable.
- (decision) CLI surface: wikis and agents-install verbs are lazy-loaded subcommand modules in bin/lumina.js (cold-start guard); VALID_AGENT_TARGETS is a separate set from VALID_IDE_TARGETS; agent-target selection is never persisted into a project lumina.config.yaml (global concern, no project payload).
- (decision) lumi-hub skill source at src/skills/agents/hub/ [ASSUMPTION: subtree name], canonicalId lumi-hub, installed only by agent-host installs; must operate through the lumina wikis CLI exclusively (same no-direct-registry rule).
- (decision) All new subcommands follow the existing JSON/exit contract: JSON to stdout under --json, {error,code} to stderr, codes 0/1/2/3/4. [ADOPTED extension]
- (version) No new runtime dependencies; stack inherited as pinned in package.json: Node >=20 ESM, commander ^12.1.0, @clack/prompts ^0.9.1, js-yaml ^4.1.0, glob ^11.0.0, picocolors ^1.1.1. No web re-verification needed - nothing new is introduced.
- (constraint) Inherited (project-context section 3 + v0.1 ADD): atomicWrite everywhere, safePath for relative fragments, exit-code contract, lazy imports / cold-start <300ms, no native modules, no postinstall, devDependencies empty, raw/ additions-only, wiki.mjs sole graph mutator, append-only log, idempotency CI gate, plain-language rule 21, multi-language docs sync, no emoji, zero telemetry, LoC soft cap 3000.
- (assumption) Windows support for global paths: ~/.lumina and platform skill dirs resolved via os.homedir()/path.join; OpenClaw/Hermes global dirs on Windows follow their docs at implementation time.
- (question) Open: exact flag name for agent installs (--agents openclaw,hermes on install vs dedicated subcommand) - spec says own flag + prompt group; final CLI spelling left to epic.
- (direction) Deferred to epics/implementation: prompt-group copy and acknowledgment text, cron/heartbeat recipe docs, size-limit UX (Telegram 20MB, OC mediaMaxMb), Hermes non-Docker remote backends, multi-agent locking, fuzzy resolve, registry backup, lumi-hub output i18n.
- (event) Fast-path draft complete in memlog; distilling spine next.
- (event) Reviewer gate ran: lint 0 findings; 3 subagent lenses returned 17 findings (reality 2, adversary 10, rubric ~7 incl overlaps). Triage: all applied as clear fixes except the verify-rename which also touches the spec (surfaced to user).
- (decision) Skill projection mechanism CHANGED (fixes reality-2, adversary L1/H3-adjacent, rubric H1): skills are NEVER passed through the template engine (bodies contain live {{ENTRY_TEXT}}-style placeholders the engine would blank). Agent-host projection = literal substitution of the canonical opening line (Read README.md at the project root...) with the rendered librarian preamble partial; classic installs stay byte-copy. Global skill installs are plain copyDir - no symlink ladder (foreign-owned dirs, platform watchers, Windows). New separate CI gate ci-agent-host-isolation.mjs [ASSUMPTION name]: classic outputs byte-identical with vs without agent targets; ci-idempotency does NOT cover this.
- (decision) normalizeKey pinned (adversary C1/M3): one shared function exported from registry.js - NFC, casefold, strip diacritics, kebab - used to compute stored keys at add AND to normalize stored values at every resolve comparison; add rejects an alias/name that already resolves to a different wiki (exit 1).
- (decision) Hub-state ownership extended (adversary C2, rubric H2): one owner module per file - registry.js owns wikis.json; new agents-manifest.js [ASSUMPTION name] owns ~/.lumina/agents/<platform>-manifest.json with pinned schema {version:1, platform, skills:[canonicalId], installedAt}; command code mutates only through these modules.
- (decision) Verb rename (adversary C3): no wikis verify - structural check folds into doctor <name> [--fix]; the word verify is reserved for the shipped /lumi-verify semantic fact-checker; inherited-invariants row added. Requires spec sync (CAP-4 wording + registry-and-cli.md) - offered to user.
- (decision) Repair semantics (adversary H1): fix creates missing directories via mkdir and seeds missing files by invoking the installer's own template render for exactly those paths - never hand-written blank files, never touching existing paths.
- (decision) layout.js anti-drift (adversary H2): colocated layout.test.js performs a real sandbox install and asserts consistency between layout data and installer output; test named in structural seed.
- (decision) packs freshness (adversary M1): resolve refreshes the registry packs field from the live wiki manifest as a side effect of every successful resolve; the routing preamble may pack-gate only on post-resolve data; registry packs is display/advisory otherwise.
- (decision) doctor JSON shape pinned (adversary M2): {schemaVersion:1, wikis:[{key,path,reachable,hasManifest,structureOk,lintOk,issues[]}]} - part of the AD-11 contract lumi-hub is written against.
- (decision) AD-8 reaches the real defect (reality-3, rubric C1): named change site src/installer/commands.js::uninstallCommand - replace unconditional rm of .agents with manifest-driven per-canonicalId removal mirroring the existing lumi-prefix-filtered .claude/skills loop; global deselect reconciliation mirrors reconcileRemovedIdeTargets (detail deferred to epic).
- (decision) Ship surface (rubric C2): package.json files allowlist enumerates installer files individually - registry.js, wikis-command.js, layout.js, agents-manifest.js must be added, plus ci-package.mjs required-present entries; test files stay excluded. Folded into AD-9.
- (decision) Hub language pre-resolution (rubric H3): before any wiki is resolved, lumi-hub mirrors the language of the user's chat message; once inside a wiki, that wiki's configured communication_language governs. No language field added to the registry.
- (decision) Test strategy (rubric M2): standard colocated node --test files for all new modules (registry.test.js, wikis-command.test.js, layout.test.js, agents-manifest.test.js); no CI-matrix changes beyond existing Windows path conventions.
- (event) Spine re-distilled as v2 with amended AD-2/3/5/6/7/8/9/10/11, new inherited row for /lumi-verify, doctor-only verb set, updated seed/map/deferred.
- (event) User delegated final review; ASSUMPTION file names accepted as proposed (registry.js, agents-manifest.js, wikis-command.js, layout.js, skills/agents/hub, templates/partials/librarian-preamble.md, scripts/ci-agent-host-isolation.mjs). Spec synced. Spine finalized.
- (event) spine finalized
